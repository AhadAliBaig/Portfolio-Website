This folder is gitignored. It holds files that are not part of the live portfolio build:

- NEW UI/     — Google AI Studio export (reference only)
- version 1/  — Previous React app source before the current redesign
- components.json — shadcn CLI config (unused by current UI)
- debug.log, thingstodo.txt, TODO.md — scratch / notes
- deploy.sh   — optional local deploy script (copy back to repo root if you need it in CI)
